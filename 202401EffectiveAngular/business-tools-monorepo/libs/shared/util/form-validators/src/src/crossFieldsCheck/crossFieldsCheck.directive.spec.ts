import { CrossFieldsCheckDirective } from './crossFieldsCheck.directive';

describe('CrossFieldsCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossFieldsCheckDirective();
    expect(directive).toBeTruthy();
  });
});
